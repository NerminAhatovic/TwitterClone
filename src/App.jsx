var React = require('react');
var ReactDOM = require('react-dom');
var TwitterContainer = require('./components/TwitterContainer.jsx');

ReactDOM.render(
  <TwitterContainer />,
  document.getElementById('content')
);
